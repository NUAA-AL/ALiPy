
# ---------------Multi label index---------------

# query the 4th, 5th labels of the 2nd instance
queried_index = (1, [3, 4])	
queried_index = (1, [3])
queried_index = (1, 3)
queried_index = (1, (3))
queried_index = (1, (3, 4))
# query all labels
queried_index = (1, )   

# -------------MultiLabelIndexCollection----------

# -------------Multi-label tools-------------